[buildout]
parts =
    python
    django
eggs =
    django-sekizai
develop =
    ../

[python]
recipe = zc.recipe.egg
interpreter = python
eggs = ${buildout:eggs}


[django]
recipe = djangorecipe
version = 1.2.1
project = testapp
settings = settings
test =
    testapp
eggs = ${buildout:eggs}